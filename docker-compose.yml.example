version: '3'

services:
  # 示例配置，实际使用时会由 docker-bot.sh 自动生成
  # bot1:
  #   build: .
  #   container_name: crisp_bot_1_example
  #   restart: unless-stopped
  #   volumes:
  #     - /opt/crisp_bot/example/config.yml:/app/config.yml
  #     - /opt/crisp_bot/example/data:/app/data
  #     - /opt/crisp_bot/example/session_mapping.yml:/app/session_mapping.yml
  #   environment:
  #     - TZ=Asia/Shanghai 
